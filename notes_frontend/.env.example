# Frontend environment variables (copy to .env and adjust values as needed)

# Base URL for backend API used by the frontend client (src/main.jsx -> getApiBaseUrl()).
# Example for local backend:
VITE_API_BASE_URL=http://localhost:8000

# Optional: Public URL where the dev server is accessible (used to configure Vite HMR and origin).
# Set this in proxied environments (e.g., Codespaces/remote CI proxies).
# Example:
# VITE_PUBLIC_URL=https://vscode-internal-22982-qa.qa01.cloud.kavia.ai:3000
